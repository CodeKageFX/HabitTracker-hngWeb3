import SplashScreen from "@/components/shared/SplashScreen";

/**
 * Route Markers:
 * "/dashboard"
 * "/login"
 * Splash Delay: SPLASH_DELAY_DEFAULT_2000
 */

export default function Home() {
  return <SplashScreen />
}